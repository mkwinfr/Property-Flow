import PlaceholderPage from '../../components/PlaceholderPage/PlaceholderPage';

const AdminUsers = () => {
  return (
    <PlaceholderPage
      title="Users & Roles"
      subtitle="Manage user accounts and permissions"
      description="Create users, assign roles, and manage access permissions. (No role restrictions currently enforced - all users have admin access)"
    />
  );
};

export default AdminUsers;
