import PlaceholderPage from '../../components/PlaceholderPage/PlaceholderPage';

const AdminNotifications = () => {
  return (
    <PlaceholderPage
      title="Notifications"
      subtitle="Configure notification rules and preferences"
      description="Set up email, SMS, and in-app notification triggers and templates."
    />
  );
};

export default AdminNotifications;
