import PlaceholderPage from '../../components/PlaceholderPage/PlaceholderPage';

const MaintenanceCosts = () => {
  return (
    <PlaceholderPage
      title="Costs & Estimates"
      subtitle="Track maintenance costs and budget estimates"
      description="Monitor spending, compare estimates, and manage maintenance budgets."
    />
  );
};

export default MaintenanceCosts;
