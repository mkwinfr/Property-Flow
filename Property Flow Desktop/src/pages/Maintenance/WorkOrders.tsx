import PlaceholderPage from '../../components/PlaceholderPage/PlaceholderPage';

const MaintenanceWorkOrders = () => {
  return (
    <PlaceholderPage
      title="Work Orders"
      subtitle="Manage all maintenance work orders"
      description="Create, assign, and track work orders across all properties."
    />
  );
};

export default MaintenanceWorkOrders;
