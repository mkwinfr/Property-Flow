import PlaceholderPage from '../../components/PlaceholderPage/PlaceholderPage';

const MaintenanceInspections = () => {
  return (
    <PlaceholderPage
      title="Inspections"
      subtitle="Schedule and track property inspections"
      description="Manage move-in, move-out, and periodic property inspections."
    />
  );
};

export default MaintenanceInspections;
