import PlaceholderPage from '../../components/PlaceholderPage/PlaceholderPage';

const DashboardAlerts = () => {
  return (
    <PlaceholderPage
      title="Alerts & Issues"
      subtitle="Active alerts and critical issues requiring attention"
      description="Monitor urgent maintenance requests, lease expirations, and other critical notifications."
    />
  );
};

export default DashboardAlerts;
